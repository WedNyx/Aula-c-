const fs=require("node:fs"),assert=require("node:assert/strict");const app=fs.readFileSync("src/App.jsx","utf8"),kv=fs.readFileSync("api/kv.js","utf8"),moods=fs.readFileSync("src/lib/checkinMoods.js","utf8"),notebook=fs.readFileSync("src/components/LearningModals.jsx","utf8"),bubble=fs.readFileSync("src/components/TeacherNyxAlertBubble.jsx","utf8");assert.match(app,/activityAttempts\?\.\[todayKey\(\)\]/);assert.match(app,/activityAttempts:\s*activityAttempts/);assert.match(kv,/next\.activityAttempts/);assert.match(app,/deliveryHistory:\[\.\.\.\(current\.deliveryHistory\|\|\[\]\), delivery\]/);assert.match(notebook,/Histórico de envios/);assert.match(notebook,/Rascunho/);assert.match(moods,/nyxWelcomeForMood/);assert.match(bubble,/Avisos do Nyx/);assert.match(bubble,/Marcar como visto/);assert.match(bubble,/Ver aluno/);console.log("Histórico, tentativa única e avisos do Nyx conectados.");