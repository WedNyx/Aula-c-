const fs=require("node:fs"),assert=require("node:assert/strict");const app=fs.readFileSync("src/App.jsx","utf8"),menu=fs.readFileSync("src/components/DashboardActionMenu.jsx","utf8"),css=fs.readFileSync("src/redesign.css","utf8");assert.match(app,/DashboardActionMenu label="Ajustes"/);assert.match(app,/DashboardActionMenu label="Mais"/);assert.doesNotMatch(app,/className="student-provider-health"/);assert.match(menu,/aria-haspopup="menu"/);assert.match(menu,/Escape/);assert.match(css,/@media\(max-width:700px\)/);assert.match(app,/Estado do Nyx/);console.log("Cabeçalhos organizados e menus acessíveis.");